# Aether — pinned to the versions verified installed in this repo's venv
# (`pip list`, 2026-09). Every pin below matches the active environment; PyPI
# has newer majors (streamlit 1.64, langchain 1.4.1, langchain-google-genai
# 4.4.0, faiss-cpu 1.15) but langchain 1.x moves the chain constructors to
# `langchain-classic`, so upgrading langchain requires the import fallback in
# app.py to be exercised. Bump deliberately, not blindly.

# --- UI ---
streamlit==1.44.1

# --- LangChain core + orchestration (0.3.x line: create_history_aware_retriever
#     lives in langchain.chains; 1.x moved it to langchain-classic) ---
langchain==0.3.27
langchain-core==0.3.76
langchain-community==0.3.27
langchain-text-splitters==0.3.11

# --- LLM: Google Gemini ---
langchain-google-genai==2.1.12

# --- Embeddings: HuggingFace all-MiniLM-L6-v2 ---
langchain-huggingface==0.3.1
sentence-transformers==5.0.0

# --- Vector store ---
faiss-cpu==1.11.0

# --- PDF parsing ---
pypdf==5.9.0

# --- Misc ---
python-dotenv==1.1.1
